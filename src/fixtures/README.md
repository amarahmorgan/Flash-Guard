# Fixtures

This folder contains reusable Playwright fixtures and shared test setup helpers.

## Current contents

- `pages.fixture.ts` — exposes reusable page objects to UI tests.

## Purpose

Fixtures reduce duplication and make tests easier to maintain by centralising setup for page objects and shared test context.